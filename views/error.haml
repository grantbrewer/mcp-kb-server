%h1 Error

.error-container
  %p.error-message= @error

  - if @validation_errors && !@validation_errors.empty?
    %h2 Validation Errors:
    %ul.validation-errors
      - @validation_errors.each do |field, messages|
        %li
          %strong= "#{field}:"
          = messages.join(', ')

%p
  %a.btn{href: '/articles'} Back to All Articles
  %a.btn{href: '/new'} Create New Article

:css
  .error-container {
    background: #fee;
    border: 2px solid #fcc;
    border-radius: 8px;
    padding: 20px;
    margin: 20px 0;
  }
  .error-message {
    color: #c00;
    font-size: 16px;
    font-weight: bold;
  }
  .validation-errors {
    background: #fff;
    padding: 15px;
    border-radius: 4px;
    margin-top: 10px;
  }
  .validation-errors li {
    margin: 5px 0;
    color: #c00;
  }
