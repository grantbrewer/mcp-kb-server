%h1 Knowledge Base Articles

%a.btn{href: "/new"} + New Article

%table
  %tr
    %th ID
    %th Title
    %th Author
    %th Category
    %th Status
    %th Created
    %th Actions
  - @articles.each do |article|
    %tr
      %td= article.id
      %td
        %a{href: "/articles/#{article.id}"}= article.title
      %td= article.author
      %td= article.category
      %td
        - if article.is_published
          %span.status-published ✓ Published
        - else
          %span.status-draft Draft
      %td= article.created_at.strftime('%Y-%m-%d %H:%M') rescue 'N/A'
      %td.actions
        %a.btn-small{href: "/articles/#{article.id}/edit"} Edit
        %form.inline-form{method: "POST", action: "/articles/#{article.id}/delete", onsubmit: "return confirm('Are you sure you want to delete this article?');"}
          %button.btn-small.btn-danger{type: "submit"} Delete

%p
  %a{href: "/"} Back to API Info
